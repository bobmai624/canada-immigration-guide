document.getElementById('print')?.addEventListener('click',()=>{document.querySelectorAll('details').forEach(x=>x.open=true);window.print()});
document.getElementById('source-search')?.addEventListener('input',e=>{const q=e.target.value.toLowerCase().trim();let count=0;document.querySelectorAll('.source').forEach(x=>{x.hidden=!x.textContent.toLowerCase().includes(q);if(!x.hidden)count++});let status=document.getElementById('search-status');if(!status){status=document.createElement('p');status.id='search-status';status.setAttribute('aria-live','polite');e.target.parentElement.after(status)}status.textContent=count?'找到 '+count+' 条来源':'没有匹配的来源，请换一个关键词。'});
document.getElementById('budget')?.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.target),t=Number(d.get('tuition')),l=Number(d.get('living')),o=Number(d.get('other')),fx=Number(d.get('fx'));const cad=t+l*12+o;document.getElementById('budget-result').textContent='演示全年支出：'+cad.toLocaleString('zh-CN')+' 加元 ≈ '+Math.round(cad*fx).toLocaleString('zh-CN')+' 元人民币（未扣预期工作收入）';});
